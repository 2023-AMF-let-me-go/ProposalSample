// let backendHost;

// const hostname = window.location.href;

// if (hostname.indexOf('/proposal') > 0 /*|| hostname.indexOf("/~~~") > 0*/) {
// 	backendHost = 'http://localhost:8080';
// } else if (hostname.indexOf('/RequirementBoard') > 0) {
// 	backendHost = 'http://localhost:8081';
// }

// export let API_BASE_URL = `${backendHost}`;
